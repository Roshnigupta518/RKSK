import { wp } from "../ResponsiveSize";

const size = {
    extraHead : wp(24),
    heading: wp(20),
    subheading: wp(18),
    title: wp(16),
    subtitle: wp(14),
    label: wp(12),
  };

  export default size;